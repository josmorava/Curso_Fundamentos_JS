var edad = 20;
//edad = edad + 1
edad +=1 // va a incrementar en 1

var peso = 60 
//peso = peso - 2
peso -= 2//Resta en 2

var burger = 2
peso = peso + burger

var precioVino = 200.3
var total = Math.round(precioVino*100 * 3) / 100//Funcion para redondear
var totalStr = total.toFixed(3)//Mostrar en patalla el nmr de decimales, tomar en cuenta dentro de los parametros la cantidad de decimales. MUESTRA EL RESULATDO EN STRINGS

var total2 = parseFloat(totalStr)//transforma a elementos numericos, decimales